# Workspace User Directives & Rules

## GitHub Pushing Policy
- **DO NOT** push changes to GitHub automatically after every small edit or feature.
- Always keep all code changes local for testing.
- Only push to GitHub when the user explicitly requests to push.
